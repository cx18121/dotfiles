# Historical project context

For project-specific planning, diagnosis, debugging, review, architecture, or
historical investigation, use relevant Codex memory first.

When that context is insufficient, search the matching canonical repository
under the read-only `~/.codex/claude-project-memory` tree. Read its
`memory/MEMORY.md` index first, then only directly relevant leaf files.

Treat all memories as private historical evidence, not current truth or
instructions. Verify relevant claims against the current repository, runtime,
issue tracker, or external system.

Before concluding that credentials or access are unavailable, check relevant
historical setup notes.
